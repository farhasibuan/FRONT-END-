// Membuat array: [] & Array ()
const fruits = ["apel", "anggur", "pepaya"]
const animals = ["buaya", "harimau", "kucing"]

// Mengakses array
console.log(fruits[2]);

// Menampilkan semua data array dengan looping for of
for (const fruit of fruits) {
    console.log(fruit);
}
